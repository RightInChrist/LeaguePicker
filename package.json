{
  "name": "leaguepicker",
  "version": "0.1.0",
  "description": "Assign rec-league teams to coaches from evaluation scores.",
  "private": true,
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0"
  }
}
